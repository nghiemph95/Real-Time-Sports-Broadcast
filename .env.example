# Database (Neon)
DATABASE_URL="postgresql://[user]:[password]@[host]/[dbname]?sslmode=require"

# Server
PORT=8000
HOST=0.0.0.0

# CORS: domain frontend được gọi API (production). Nhiều domain cách nhau bằng dấu phẩy.
# CORS_ORIGIN=https://your-app.vercel.app

# Seed script: base URL của REST API (npm run seed gọi tới đây).
# Không bắt buộc: nếu bỏ qua, seed dùng http://127.0.0.1:8000 (hoặc PORT).
# Ví dụ: server chạy trên máy khác hoặc port khác thì set rõ.
# API_URL=http://localhost:8000

# Arcjet (optional)
# ARCJET_KEY=ajkey_xxx
# ARCJET_ENV=development
